{
  "name": "hopverkefni1",
  "version": "1.0.0",
  "description": "desc",
  "main": "index.js",
  "scripts": {
    "browser-sync": "browser-sync start --server --files index.html styles.css",
    "sass-watch": "node-sass --source-map-embed -w scss -r -o ."
  },
  "author": "jac",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "node-sass": "^4.9.4"
  }
}
